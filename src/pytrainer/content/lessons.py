"""Контент уроков. Все строки — Markdown."""
from __future__ import annotations

from . import Lesson

# ----- Раздел 0: Знакомство -----

S0 = ("00-intro", "0. Знакомство")

L_00_how_to_learn = Lesson(
    id="00-how-to-learn",
    section_id=S0[0],
    section_title=S0[1],
    title="Как заниматься по этому курсу (читать первым!)",
    body_md="""\
## Зачем эта инструкция

Главная ошибка новичков — **читать уроки как книгу**: глазами по тексту,
ничего не пробуя. Через 30 минут чтения кажется, что всё понятно.
Через час уже нихрена не помнишь, а сесть и написать `print` — ступор.

Этот курс рассчитан на то, что **на каждый урок ты тратишь 2–3 часа** —
не на чтение, а на **работу с кодом**. Ниже — как именно.

## Один урок = одна сессия 2–3 часа

Не пытайся пробежать «один урок за вечер ради галочки». Лучше один урок
**по-настоящему** — потом перерыв в день, и следующий. Чем сильнее ты
устал — тем хуже усваивается. Если устал — это сигнал закрыть приложение,
а не продавить ещё «полчасика».

Стрик считается за **любой заход в день**: открыл, прочитал хоть полстраницы,
запустил один пример — день засчитан. Не нужно «прибежать к концу урока».

## Пошаговый алгоритм для каждого урока

Делай в этом порядке. Не перепрыгивай.

### 1. Прочитай теорию — медленно, один раз (15–25 мин)

Не пытайся ничего запомнить. Просто читай и **проговаривай вслух или про
себя ключевые слова**: «переменная — это коробка с именем», «if проверяет
условие». Если на странице есть пример кода — посмотри глазами, что в нём
происходит, **не запуская**.

Если совсем не понятно — это нормально. Иди дальше, не зависай. Понимание
догонит при практике.

### 2. Проверь незнакомые слова в Словаре (5–10 мин)

В каждом уроке есть слова в **жирном** или `моноширинном` шрифте. Зайди в
раздел «Словарь» в боковом меню и прочитай определения тех, которые ты не
помнишь. Слов будет немного — 3–5 на урок. Их и нужно усвоить.

### 3. Поиграй с «Попробуй сам» (10–20 мин)

Прямо под текстом урока есть блок **«💡 Попробуй сам»** с готовым кодом.
Сначала нажми «▶ Запустить» как есть — посмотри вывод. Потом **изменяй
по одной вещи**:

- Замени строку на свою.
- Поменяй число.
- Удали строку и посмотри, что произойдёт.
- Добавь ещё одну.

Это самый важный шаг. Каждое маленькое изменение — это микро-эксперимент:
«а если я сделаю так, что выведется?». Сначала ты предсказываешь в голове,
**потом** запускаешь и проверяешь. Если угадал — закрепил. Если нет —
понял что-то новое.

### 4. Перепечатай ключевой пример руками в «Песочница» (15–25 мин)

Боковое меню → «Песочница». Открой урок в одном окне, песочницу — в другом
(или просто переключайся). Возьми главный пример из урока и **набери его
руками с нуля**, не копируя.

Зачем: при наборе руками твои пальцы запоминают `:`, `(`, отступы — это
часть синтаксиса, которая иначе будет вызывать ошибки годами. После
перепечатывания — запусти и убедись, что вывод тот же.

Появилась ошибка? **Прочитай её спокойно.** В уроке про ошибки мы научимся
их читать; пока что — название ошибки + номер строки уже многое говорят.

### 5. Реши задачи к уроку (40–80 мин)

Снизу страницы урока — список задач. Открывай по одной.

Для каждой задачи:

1. Прочитай условие. Не пиши код сразу — **подумай 1–2 минуты, что должна
   делать программа**. Можно даже написать план на бумаге словами.
2. Открой стартовый код, прочитай комментарии.
3. Пиши решение. Запускай кнопкой «▶ Запустить» — смотри, какой вывод.
4. Когда кажется, что готово — жми «✓ Проверить».
5. **Не сошлось?** Не нервничай. Жми «💡 Подсказка» — она направит. Если
   подсказок не хватило — «📖 Показать решение» с разбором, ПОЧЕМУ так.
6. Решение увидел? Закрой его, **сотри свой код**, и напиши заново —
   уже от себя, без подглядывания. Это превращает «понятно» в «умею».

### 6. Запиши заметку своими словами (5–10 мин)

Открой раздел «Заметки» в боковом меню. Напиши 3–5 предложений: **что я
понял в этом уроке своими словами**.

Не списывай из урока. Если не получается своими словами — значит, ты не
понял, и это сигнал вернуться к шагу 1 (или задать вопрос — куда задавать,
напишу ниже).

### 7. (Опционально) Сделай задание «нагло»

«Нагло» = расширить пример. Например, в уроке про `if/else` сделай
программу, которая получает на вход возраст и пишет: до 14 — «школьник»,
14–17 — «подросток», 18–25 — «студент», 26+ — «взрослый». Этого нет в
задачах, но это самое полезное упражнение — сам придумываешь, сам делаешь.

## Что делать, если что-то непонятно

1. **Прочитай ещё раз медленно.** 70% «непоняток» — это просто пропущенное
   слово в тексте.
2. **Проверь Словарь.** Возможно, это термин, который ты пропустил.
3. **Попробуй пример в Песочнице.** Часто абстрактное «что такое цикл»
   щёлкает только когда ты сам ввёл `for i in range(5)` и увидел вывод.
4. **Спроси меня (Devin) в чате**, если работаешь со мной. Я объясню
   тем, как именно ты учишься. Опиши, что именно непонятно, и где затык —
   общие «непонятно» бесполезны.
5. **Не двигайся дальше через силу.** Если урок про условия не зашёл —
   циклы будут полным мраком. Лучше потратить ещё час на условия, чем
   месяц мучаться позже.

## Главные правила

- **Печатай руками, не копируй.** Это правило #1.
- **Запускай каждый пример.** Чтение без запуска — пустая трата времени.
- **Ломай примеры специально.** Удаляй кавычку, ставь лишнюю скобку,
  меняй имя переменной. Учись читать ошибки.
- **Делай перерывы.** 25 минут работы → 5 минут отдыха. Через 2–3 цикла —
  длинный перерыв.
- **Веди заметки.** Будущий ты через месяц очень благодарен текущему тебе.
- **Не торопись.** Курс идёт от нуля. Если кажется «слишком легко» в
  первых уроках — отлично, значит, фундамент кладётся правильно.

## Что в этом приложении полезно знать сразу

| Раздел | Зачем |
|---|---|
| **Курс** | Уроки и задачи по порядку. Левая колонка — оглавление. |
| **Песочница** | Любой свой код. Запускается за 2 секунды. Нечего бояться. |
| **Карточки** | Быстрый квиз на 5 минут перед сном — повторить термины. |
| **Заметки** | Тетрадь по урокам. Своими словами. |
| **Словарь** | 40+ терминов из курса с простыми объяснениями. |
| **Достижения** | Геймификация. Маленькие медальки за этапы. |
| **Настройки** | Тёма, размер шрифта, имя для приветствия. |

## Готов?

Открывай следующий урок «Что такое Python и зачем он» — он короче этого
и приятнее. После — двигайся по курсу слева направо: от `print` до
своего Telegram-бота. Поехали.
""",
    try_it_code=None,
    task_ids=(),
    xp=2,
)

L_00_what = Lesson(
    id="00-what-is-python",
    section_id=S0[0],
    section_title=S0[1],
    title="Что такое Python и зачем он",
    body_md="""\
## О чём вообще речь

Python — это **язык программирования**. Это набор правил, на котором ты
рассказываешь компьютеру, что делать. Компьютер не понимает русского — ему
нужны команды в строгом формате. Python — один из самых дружелюбных таких
форматов: он специально создан, чтобы код был похож на обычный английский.

Вот пример программы на Python:

```python
name = "Кирилл"
age = 19
print("Привет,", name, "тебе", age)
```

Если ты прочитал это и в принципе понял, что тут происходит — значит, у тебя
уже есть интуиция. Через несколько часов это станет твоим обычным языком.

### Зачем Python именно тебе

В опросе ты выбрал «работа в IT, бот — повод втянуться». Это правильный
выбор — Python:

- Очень популярен среди работодателей. На нём пишут бекенды банков, сайтов,
  видеохостингов, рекомендательных систем, анализ данных.
- Лидер по машинному обучению и AI: PyTorch, TensorFlow, scikit-learn — всё
  это Python-библиотеки.
- На нём проще всего написать **первый Telegram-бот** (через 8 разделов мы
  это и сделаем).
- Часто используется для автоматизации рутины: «обработай 200 файлов»,
  «выгрузи отчёт из Excel», «скачай прайсы с 10 сайтов».

Для каждой из этих задач — есть готовая библиотека (это набор кода, который
кто-то уже написал и которым можно пользоваться). Так что **большая часть
работы программиста на Python — это собрать программу из готовых кусков**, а
не писать всё с нуля.

### Что такое «код» и «программа»

Давай разберёмся в словах. Тебе встретятся:

- **Код** — текст, который ты пишешь. Просто буквы в файле.
- **Программа** — то, что получается после запуска кода. То, что выполняется
  в памяти компьютера.
- **Скрипт** — чаще всего то же, что программа, но обычно одно-двухфайловая
  и решает узкую задачу. «Скрипт для выгрузки прайса» — это маленькая
  программа.
- **Интерпретатор** — собственно «движок» Python. Это программа на
  компьютере, которая читает твой код и выполняет его. Когда ты запускаешь
  `python my_script.py` — ты говоришь интерпретатору «прочитай и выполни».
- **Синтаксис** — правила, как именно писать код. Где ставить скобки, где
  пробелы, где двоеточия. Если нарушил — Python скажет «SyntaxError».

Эти слова будут попадаться в каждом уроке. Не пытайся выучить их наизусть —
они сами запомнятся через пару уроков.

### Как Python запускает твой код

В упрощённом виде:

1. Ты пишешь файл `my_script.py` (расширение `.py` обозначает Python-файл).
2. Запускаешь команду `python my_script.py`.
3. Интерпретатор читает файл **сверху вниз**, по одной инструкции за раз.
4. Каждая инструкция выполняется (или вызывает ошибку — тогда выполнение
   останавливается).
5. Когда дошёл до конца файла — программа завершается.

В нашем приложении тебе **не нужно** писать файлы и запускать что-то в
терминале — ты можешь писать прямо в окошке «Песочница» или в окне задач, а
приложение запустит код за тебя. Но знать, **как это работает «по-настоящему»**,
полезно: реальные программы запускаются именно из файлов.

### Как читать этот курс

В нашем приложении уроки построены так:

1. **Текст** — то, что ты сейчас читаешь. Объясняем тему по шагам.
2. **Примеры кода** в подсветке. Их можно копировать и пробовать в
   «Песочнице».
3. **«Попробуй сам»** — есть готовый код прямо под уроком, его можно
   запустить кнопкой и посмотреть результат. Можно править его и пробовать
   разное.
4. **Задачи** — отдельные карточки после урока. Их нужно решать самому,
   приложение проверит правильность авто-тестами. За решение даём XP.
5. **Достижения** появляются автоматически — это как маленькие медальки за
   шаги вперёд.

### Сколько на это уйдёт времени

Каждый урок устроен так, что один заход — это **2–3 часа** (теория + 2–3
задачи + эксперименты в песочнице). Если есть час — пройди половину урока,
потренируйся на одной задаче. Главное — практика. Чтение без кода почти
ничего не даёт.

### Запомнить

- Python — язык программирования; его «знать» = уметь читать и писать на нём код.
- Программа — это инструкция компьютеру, читается сверху вниз.
- Сломать ничего нельзя: твой код выполняется в безопасной песочнице.
- Главное правило обучения: **печатать код руками, а не копировать**. Так
  он действительно запоминается.

Готов? Дальше — первая команда `print` и куча примеров с ней.
""",
    try_it_code='print("Hello, world!")\nprint("Я учу Python")\nprint("Это моя первая программа.")\n',
    task_ids=(),
    xp=3,
)

L_00_print = Lesson(
    id="00-first-print",
    section_id=S0[0],
    section_title=S0[1],
    title="Первая команда: print",
    body_md="""\
## Что такое функция (на пальцах)

`print` — это **функция**. Считай функцию волшебным ящиком, у которого:

- есть **имя** (`print`),
- ему **что-то даёшь** (в круглых скобках после имени),
- он **что-то делает** (или возвращает результат).

У `print` задача простая: **показать на экране** всё, что ему дали.

```python
print("Привет!")
print("Меня зовут Кирилл")
```

> Заметь: круглые скобки `(` и `)` — обязательны. Без них Python не поймёт, что
> ты вызываешь функцию. `print "Привет"` — будет ошибка.

### Попробуй сам №1

Открой «Песочница» (значок в сайдбаре) и набери **руками** (не копируй):

```python
print("Меня зовут <твоё имя>")
print("Я учусь Python")
```

Запусти. Увидь две строки в выводе. **Молодец, ты только что написал свою первую
программу.**

---

## Несколько значений в одном `print`

В скобки можно передать **несколько** аргументов через запятую — Python между
ними поставит **пробел**:

```python
print("2 + 2 =", 2 + 2)
# 2 + 2 = 4

print("Меня зовут", "Кирилл", "и мне", 30, "лет")
# Меня зовут Кирилл и мне 30 лет
```

Обрати внимание: число `2 + 2` Python **сначала посчитал**, и только потом вывел
`4`. Внутри скобок можно писать любые выражения — он их сначала вычислит.

### Попробуй сам №2

Что выведет код ниже? Сначала **подумай**, потом запусти в Песочнице:

```python
print("3 *", 4, "=", 3 * 4)
print("Длина 'Python':", len("Python"))
```

(`len` — ещё одна встроенная функция, она считает длину строки. Скоро её
разберём подробно.)

---

## Параметр `sep` — твой первый «именованный аргумент»

Тот пробел между аргументами, что Python ставит сам — это «разделитель», по-английски
**separator**. Его можно поменять **именованным параметром** `sep`:

```python
print("a", "b", "c", sep="-")
# a-b-c

print("2025", "11", "08", sep="/")
# 2025/11/08

print("эта", "строка", "очень", "плотная", sep="")
# этастрокаоченьплотная
```

Запись `sep="-"` читается так: «параметр `sep` равен дефису». Это и есть
**именованный аргумент** — он даёт значение по имени параметра, а не по
порядку. Дальше в курсе таких будет много.

### Параметр `end` — что в конце

По умолчанию `print` после вывода делает **перенос строки** (`\\n`). Это можно
заменить:

```python
print("Привет", end="!")
print(" Как дела?")
# Привет! Как дела?
```

Видишь? Без `\\n` в конце вторая строка приклеилась к первой.

### Попробуй сам №3

В Песочнице добейся, чтобы код вывёл **ровно** строку:

```
2025-11-08
```

Подсказка: используй `sep="-"` и три аргумента.

---

## Зачем `print` нужен на самом деле

`print` — это твой главный **инструмент отладки**. Слово «дебаг» (от англ.
*debug* — «выгнать ошибку») означает «искать, где код пошёл не туда». Самый
простой способ — расставить `print` в подозрительных местах:

```python
x = 10
print("x перед изменением:", x)
x = x + 5
print("x после:", x)
```

Когда программа сломается — ты по `print`-ам сразу увидишь, **на каком шаге**
значение стало не таким, какое ожидал. Эту привычку выработай сразу: не
понимаешь, что происходит — поставь `print`.

---

## Самопроверка

Ответь себе (можно вслух):

1. Что такое **функция**? (Ящик с именем, в который что-то даёшь.)
2. Зачем нужны **круглые скобки** после `print`?
3. Что делает параметр `sep`?
4. Что делает параметр `end`?
5. Зачем нужен `print`, если у меня и так код «должен работать»?

Если хоть на один вопрос ответ «не помню» — вернись на нужный раздел выше.
""",
    try_it_code='print("Меня зовут Python")\nprint("Сегодня", "хороший", "день", sep=" — ")\nprint("Конец строки", end="!\\n")\n',
    task_ids=("t-print-intro",),
    xp=4,
)

L_00_errors = Lesson(
    id="00-errors-comments",
    section_id=S0[0],
    section_title=S0[1],
    title="Комментарии и первые ошибки",
    body_md="""\
## Комментарии — заметки в коде

**Комментарий** — это текст, который Python **игнорирует**. Он не выполняется,
не печатается, ни на что не влияет. Зачем нужен? Оставить заметку:

- себе через месяц («что вообще тут происходит?»),
- другому человеку, который будет читать твой код,
- как «закладку», чтобы временно отключить кусок кода (не удалять — закомментировать).

В Python комментарий начинается со знака `#`:

```python
# Это комментарий, Python его не выполняет
print("А вот это — выполнит")  # и это после кода — тоже комментарий
```

Что бы ты ни написал после `#` до конца строки — Python это пропустит.

### Многострочный комментарий

В Python нет специального синтаксиса для многострочного комментария. Просто
ставят `#` в начале каждой строки:

```python
# Это длинное пояснение
# в три строки.
# Каждая начинается с решётки.
```

### Закомментировать код

Это очень частый приём — «выключить» строку, не удаляя её:

```python
print("Эта строка работает")
# print("А эта — временно выключена")
```

В большинстве редакторов есть горячая клавиша `Ctrl+/` — она комментирует
выделенные строки автоматически.

### Попробуй сам №1

В Песочнице запусти этот код:

```python
print("Видно")
# print("Не видно")
print("Тоже видно")
```

Убери `#` перед второй строкой и запусти снова. Что изменилось?

---

## Первые ошибки — твои союзники

Когда Python натыкается на что-то, чего не понимает, он не паникует и не
ломается. Он печатает в консоль **traceback** — буквально «обратный след»: где
и что пошло не так.

### Пример: незакрытая кавычка

```python
print("Привет)
```

Получим:

```
  File "<stdin>", line 1
    print("Привет)
                  ^
SyntaxError: EOL while scanning string literal
```

Переведём по частям:
- **`SyntaxError`** — «ошибка синтаксиса». Это значит, что Python не смог даже
  прочитать твой код, не то что выполнить.
- **`EOL while scanning string literal`** — «конец строки во время чтения
  строкового литерала». Перевод на человеческий: «я искал закрывающую кавычку,
  а нашёл конец строки».
- **`^`** — Python показывает **столбик**, в котором заметил беду.

Перевёл → нашёл → поправил. Готово.

---

## Топ-5 ошибок, которые ты увидишь чаще всего

| Сообщение | Что это значит | Как чинить |
|---|---|---|
| `SyntaxError` | Python вообще не смог прочитать строку | Посмотри на стрелочку `^` — там грамматика |
| `NameError: name 'x' is not defined` | Используешь имя, которого ещё нет | Опечатка? Переменная не создана? |
| `TypeError: ...` | Значение есть, но **не того типа** | Например, складываешь строку и число |
| `ValueError: ...` | Тип правильный, **значение неправильное** | Например, `int("abc")` — нельзя |
| `IndentationError` | Сбился отступ (пробелы в начале строки) | Выровняй отступ, **4 пробела** на уровень |

### Пример: `NameError`

```python
print(name)  # name ещё не существует!
# NameError: name 'name' is not defined
```

Чинится так:

```python
name = "Кирилл"
print(name)
```

### Пример: `TypeError`

```python
print("Мне " + 30 + " лет")
# TypeError: can only concatenate str (not "int") to str
```

`30` — это число (`int`), а `+` со строкой требует другую строку. Чиним:

```python
print("Мне " + str(30) + " лет")
# или лучше через f-строку (узнаем в следующем разделе):
print(f"Мне {30} лет")
```

---

## Как читать сообщение об ошибке (алгоритм)

1. **Не закрывай терминал в панике.** Ошибка — это полезный текст.
2. **Посмотри на последнюю строку** — там тип ошибки (`NameError`, `TypeError`...) и
   короткое описание.
3. **Посмотри на строку с `File "..."` и номер строки** — там адрес «места преступления».
4. **Открой эту строку в коде** — глаз сразу зацепится за проблему.
5. **Если непонятно** — скопируй текст ошибки в Google или в чат с GPT. Это **нормально**.
   Программисты гуглят сотни раз в день.

### Попробуй сам №2

Сломай код **намеренно**. В Песочнице запусти каждый из этих фрагментов и прочитай ошибку:

```python
print(name)
```

```python
print("число " + 5)
```

```python
print("незакрытая)
```

Цель — **не починить**, а **прочитать ошибку и понять**, что она говорит.

---

## Запомни

> **Ошибка — это не провал. Это подсказка**, в каком месте что поправить.
> Программисты получают по ~50–100 ошибок в день. Это часть работы, а не повод расстраиваться.
""",
    try_it_code='# попробуй убрать решётку и посмотри, что выведется\nprint("Без комментария")\n# print("С комментарием")\n',
    task_ids=(),
    xp=3,
)


# ----- Раздел 1: Переменные и типы -----

S1 = ("01-vars", "1. Переменные и типы")

L_01_vars = Lesson(
    id="01-variables",
    section_id=S1[0],
    section_title=S1[1],
    title="Переменные — коробки для данных",
    body_md="""\
**Переменная** — это имя для значения. Делается через знак `=`:

```python
name = "Кирилл"
age = 19
```

Слева от `=` — имя. Справа — то, что в нём лежит. После этого имя можно использовать
вместо значения:

```python
print(name)
print("Мне", age, "лет")
```

### Имена

- Латинскими буквами и цифрами, начинаются с буквы или `_`.
- Не должны совпадать с ключевыми словами (`if`, `for`, `def` и т.п.).
- Принято писать через `_`: `user_name`, `total_price`.
- Чем понятнее имя — тем лучше. `x = 1500` — плохо, `monthly_income = 1500` — хорошо.

### Менять можно

Имя переменной — это «коробка». Можно положить туда другое значение в любой момент:

```python
score = 0
score = score + 10
print(score)  # 10
```

`score = score + 10` читается так: «возьми текущий `score`, прибавь 10, запиши обратно
в `score`». Можно короче:

```python
score += 10  # то же самое
```
""",
    try_it_code='''name = "Кирилл"
age = 19
print("Привет,", name)
print("Мне", age, "лет")

age = age + 1
print("А теперь мне", age)
''',
    task_ids=(),
    xp=4,
)

L_01_types = Lesson(
    id="01-types",
    section_id=S1[0],
    section_title=S1[1],
    title="Типы: число, строка, булево",
    body_md="""\
У каждого значения в Python есть **тип** — что это вообще такое.

| Тип | Что это | Пример |
|---|---|---|
| `int` | целое число | `42`, `-3`, `0` |
| `float` | дробное число | `3.14`, `-0.5` |
| `str` | строка (текст) | `"привет"`, `'42'` |
| `bool` | булево (логика) | `True`, `False` |
| `None` | «ничего» | `None` |

Узнать тип можно функцией `type`:

```python
print(type(42))      # <class 'int'>
print(type("42"))    # <class 'str'>
print(type(True))    # <class 'bool'>
```

### Числа

С числами всё понятно: `+`, `-`, `*`, `/`, `**` (степень), `//` (целочисленное деление),
`%` (остаток):

```python
print(7 // 2)   # 3
print(7 % 2)    # 1
print(2 ** 10)  # 1024
```

### Строки

Строка — текст в кавычках. Кавычки можно использовать любые: `"..."` или `'...'`. Для
многострочного текста есть тройные кавычки:

```python
greeting = "Привет!"
multiline = \"\"\"
строка 1
строка 2
\"\"\"
```

Строки можно **складывать** (склеивать):

```python
first = "Кирилл"
last = "Иванов"
full = first + " " + last
print(full)
```

И умножать на число (повторение):

```python
print("=" * 20)  # ====================
```

### f-строки (форматирование)

Удобный способ собрать строку с переменными — поставить `f` перед открывающей кавычкой
и писать переменные в фигурных скобках:

```python
name = "Кирилл"
age = 19
print(f"Привет, {name}! Тебе {age}.")
```

### Преобразование типов

```python
x = "42"        # это строка
n = int(x)      # стало 42 (число)
print(n + 1)    # 43
```

Если попытаться `int("abc")` — будет ошибка `ValueError`. Не страшно: пока что мы
будем знать, что в строке точно цифры.
""",
    try_it_code='''x = 7
y = 3
print("целое:", x // y)
print("остаток:", x % y)
print("дробное:", x / y)

s = "42"
print("это строка:", s + s)
print("это число:", int(s) + int(s))
''',
    task_ids=(),
    xp=5,
)

L_01_input = Lesson(
    id="01-input",
    section_id=S1[0],
    section_title=S1[1],
    title="Считаем ввод от пользователя — input",
    body_md="""\
## Зачем нужен `input`

До этого момента твои программы были «закрытыми»: ты их писал, запускал — и они
делали ровно то, что ты заранее написал. Но настоящие программы взаимодействуют
с пользователем: спрашивают, получают ответ, реагируют.

Функция **`input`** — это «голос» программы. Она:
1. **печатает приглашение** на экран (если ты передашь его в скобках),
2. **ждёт**, пока человек что-то введёт с клавиатуры,
3. **ждёт нажатия Enter**,
4. **возвращает** то, что ввели, как **строку**.

```python
name = input("Как тебя зовут? ")
print(f"Привет, {name}!")
```

Что произойдёт при запуске:
1. Программа напечатает: `Как тебя зовут? `
2. Курсор замрёт в ожидании. Программа **не идёт дальше**, пока ты не нажмёшь Enter.
3. Ты вводишь, например, `Кирилл` и жмёшь Enter.
4. В переменной `name` теперь лежит строка `"Кирилл"`.
5. Печатается `Привет, Кирилл!`.

> В нашей «Песочнице» в задачах ввод подаётся через специальное поле **stdin**
> (рядом с кнопкой «Запустить»). Туда можно заранее написать, что бы ты хотел
> «ввести», и программа возьмёт это автоматически.

---

## input всегда возвращает строку

Это **очень важно** запомнить. Что бы пользователь ни ввёл — даже число — `input`
вернёт это как **строку (`str`)**.

```python
age = input("Возраст: ")
print(type(age))   # <class 'str'>
print(age + 1)     # ОШИБКА: TypeError
```

Почему? Потому что Python не знает, ввёл ты число или нет. Может, у тебя там
буквы. Поэтому он по умолчанию даёт самое безопасное — текст.

### Превращаем строку в число

Если ты **уверен**, что пользователь ввёл число — оборачивай в `int(...)` или `float(...)`:

```python
age = int(input("Возраст: "))     # стало числом
height = float(input("Рост в м: ")) # стало дробным
print(f"Через 10 лет тебе будет {age + 10}")
```

Это очень частый паттерн. Запомни его наизусть:

```python
число = int(input("Подсказка: "))
дробь = float(input("Подсказка: "))
текст = input("Подсказка: ")
```

### Попробуй сам №1

Напиши в Песочнице программу:
- спроси два числа,
- напечатай их сумму, разность, произведение и частное.

Шаблон (заполни сам):

```python
a = int(input("Первое число: "))
b = int(input("Второе число: "))
print("Сумма:", ...)
print("Разность:", ...)
# и так далее
```

---

## Подвох: что если введут «двадцать»?

```python
age = int(input("Возраст: "))
# пользователь вводит: двадцать
# ValueError: invalid literal for int() with base 10: 'двадцать'
```

Это **нормально**. Защищаться от такого мы научимся позже — когда дойдём до
`try/except` (раздел про исключения). Пока что считаем, что пользователь
**вменяемый** и вводит то, что просили.

---

## input + сообщение в самом приглашении

Маленькая деталь, которая делает программу приятнее: **двоеточие и пробел**
после вопроса:

```python
# плохо — некрасиво:
name = input("Как тебя зовут?")  # курсор лип к вопросу

# хорошо:
name = input("Как тебя зовут? ")  # пробел после знака
```

И ещё хорошая привычка — называй переменную тем, что в неё кладёшь:

```python
city = input("В каком городе живёшь? ")    # хорошо
x = input("В каком городе живёшь? ")       # плохо: x — это что вообще?
```

---

## Сложим всё в маленький мини-проект

«Карточка знакомства»:

```python
print("=== Знакомство ===")
name = input("Имя: ")
city = input("Город: ")
age = int(input("Возраст: "))
print()
print(f"Привет, я {name}, мне {age}, живу в городе {city}.")
print(f"Через 10 лет мне будет {age + 10}.")
```

Это уже **полноценная программа**: спрашивает, считает, выводит. Поздравляю,
ты её написал.

### Попробуй сам №2

Расширь «карточку знакомства»:
- спроси любимое число,
- напечатай это число, его квадрат и куб (`число ** 2`, `число ** 3`).

---

## Самопроверка

1. Что возвращает `input` — строку или число?
2. Как превратить ввод в **целое** число? В **дробное**?
3. Что произойдёт, если пользователь введёт буквы вместо числа в `int(input(...))`?
4. Зачем нужен пробел после вопроса в `input("Имя: ")`?
""",
    try_it_code='''name = input("Как тебя зовут? ")
print(f"Здравствуй, {name}!")
''',
    task_ids=("t-greeting", "t-magic-8"),
    xp=5,
)


# ----- Раздел 2: Условия -----

S2 = ("02-cond", "2. Условия")

L_02_if = Lesson(
    id="02-if-else",
    section_id=S2[0],
    section_title=S2[1],
    title="if / else: первое решение",
    body_md="""\
**Условие** — точка, где программа решает: «делать одно или другое».

```python
age = 17
if age >= 18:
    print("Можно голосовать")
else:
    print("Подожди ещё")
```

### Главное

- После `if` пишется **условие** — выражение, которое даёт `True` или `False`.
- После двоеточия `:` идёт **тело** — что делать, если условие истинно. Тело сдвинуто
  пробелами вправо. **Этот сдвиг (отступ) — обязателен.** Пиши 4 пробела или один Tab,
  но не смешивай.
- `else` — что делать, если условие ложно. У `else` тоже свой блок.

### Сравнения

```text
==  равно
!=  не равно
>   больше
<   меньше
>=  больше или равно
<=  меньше или равно
```

`=` это «положить значение», `==` это «сравнить». Не путай.

```python
if name == "Python":
    print("Это твой друг")
```

### Один if без else

`else` не обязателен. Можно просто:

```python
balance = 1000
if balance < 0:
    print("Долг!")
print("Конец проверки")
```
""",
    try_it_code='''age = int(input("Возраст: "))
if age >= 18:
    print("Совершеннолетний")
else:
    print("Ещё нет")
''',
    task_ids=("t-even-odd",),
    xp=5,
)

L_02_elif = Lesson(
    id="02-elif",
    section_id=S2[0],
    section_title=S2[1],
    title="elif: цепочка условий",
    body_md="""\
Когда вариантов больше двух, появляется `elif` — «иначе если»:

```python
score = 73
if score >= 90:
    print("Отлично")
elif score >= 70:
    print("Хорошо")
elif score >= 50:
    print("Удовлетворительно")
else:
    print("Не сдал")
```

### Как читать

Python проверяет условия **по порядку сверху вниз**. Первое подошедшее — выполняется,
остальные пропускаются.

### Цепочка `elif` — это не несколько `if`

Сравни:

```python
# 1. Цепочка — выполнится одно
if x > 0:
    print("плюс")
elif x == 0:
    print("ноль")
elif x < 0:
    print("минус")

# 2. Несколько отдельных if — могут сработать несколько
if x > 0:
    print("плюс")
if x == 0:
    print("ноль")
if x < 0:
    print("минус")
```

Тут оба варианта дают одинаковый результат, потому что только одно условие может быть
истинным. Но в общем случае — **`elif` это часть одной цепочки, отдельный `if` — это
независимая проверка.**
""",
    try_it_code='''score = int(input("Балл: "))
if score >= 90:
    print("Отлично")
elif score >= 70:
    print("Хорошо")
elif score >= 50:
    print("Удовлетворительно")
else:
    print("Не сдал")
''',
    task_ids=("t-grade",),
    xp=5,
)

L_02_bool = Lesson(
    id="02-bool-ops",
    section_id=S2[0],
    section_title=S2[1],
    title="Логика: and, or, not",
    body_md="""\
Условия можно объединять.

- **`and`** (и) — истинно, если **оба** истинны.
- **`or`** (или) — истинно, если **хотя бы одно** истинно.
- **`not`** (не) — переворачивает.

```python
age = 25
has_id = True

if age >= 18 and has_id:
    print("Пускаем")

if age < 18 or not has_id:
    print("Не пускаем")
```

### Скобки

Если условий много — лучше с скобками, чтобы не путаться:

```python
if (age >= 18 and has_id) or is_admin:
    ...
```

### True и False — это значения

Их можно сохранить в переменную:

```python
is_active = age >= 18
print(is_active)  # True или False
```
""",
    try_it_code='''age = 20
has_id = True
print(age >= 18 and has_id)  # True
print(age < 18 or not has_id)  # False
''',
    task_ids=("t-password",),
    xp=4,
)


# ----- Раздел 3: Списки и словари -----

S3 = ("03-collections", "3. Списки и словари")

L_03_lists = Lesson(
    id="03-lists",
    section_id=S3[0],
    section_title=S3[1],
    title="Списки: упорядоченные коллекции",
    body_md="""\
**Список** (`list`) — упорядоченный набор значений. Записывается в квадратных скобках,
элементы через запятую. Может содержать что угодно.

```python
fruits = ["яблоко", "груша", "банан"]
print(fruits)
print(len(fruits))  # 3 — длина списка
```

### Доступ по индексу

Индекс — номер элемента. **Считаем с 0!**

```python
fruits = ["яблоко", "груша", "банан"]
print(fruits[0])   # яблоко
print(fruits[1])   # груша
print(fruits[-1])  # банан — отрицательный индекс = с конца
```

### Менять и добавлять

```python
fruits[0] = "ананас"      # заменили
fruits.append("персик")   # добавили в конец
fruits.remove("груша")    # удалили по значению
print(fruits)
```

### Срезы

`spisok[начало:конец]` — берём подсписок:

```python
nums = [1, 2, 3, 4, 5]
print(nums[1:4])   # [2, 3, 4]
print(nums[:3])    # [1, 2, 3]
print(nums[::-1])  # [5, 4, 3, 2, 1] — наоборот
```

### Полезные функции

```python
nums = [3, 1, 4, 1, 5]
print(min(nums), max(nums), sum(nums))  # 1 5 14
nums.sort()
print(nums)  # [1, 1, 3, 4, 5]
```
""",
    try_it_code='''fruits = ["яблоко", "груша", "банан"]
fruits.append("персик")
print(fruits)
print("Первый:", fruits[0])
print("Последний:", fruits[-1])
print("Длина:", len(fruits))
''',
    task_ids=("t-shopping-list",),
    xp=6,
)

L_03_dicts = Lesson(
    id="03-dicts",
    section_id=S3[0],
    section_title=S3[1],
    title="Словари: ключ → значение",
    body_md="""\
**Словарь** (`dict`) — пары «ключ — значение». Записывается в фигурных скобках:

```python
user = {
    "name": "Кирилл",
    "age": 19,
    "is_admin": False,
}

print(user["name"])      # Кирилл
print(user["age"] + 1)   # 20
```

### Зачем

Список хорош, когда у тебя **последовательность одинаковых вещей** (фрукты,
покупки). Словарь — когда **разные данные с понятными именами** (профиль, настройки).

### Менять и добавлять

```python
user["age"] = 20            # поменяли
user["email"] = "k@e.com"   # добавили новый ключ
del user["is_admin"]        # удалили
```

### Перебор

```python
for key, value in user.items():
    print(key, "→", value)
```

### Что можно положить в ключ

Текст, число, кортеж. **Список нельзя.** Звучит странно, но запомни.

### Безопасный доступ

Если ключа нет — Python кинет `KeyError`. Чтобы не ловить — `get`:

```python
print(user.get("phone"))             # None
print(user.get("phone", "не задан")) # 'не задан'
```
""",
    try_it_code='''user = {"name": "Кирилл", "age": 19}
print(user["name"], user["age"])
user["email"] = "k@example.com"
print(user)
print("Профессии нет:", user.get("job", "—"))
''',
    task_ids=(),
    xp=5,
)


# ----- Раздел 4: Циклы -----

S4 = ("04-loops", "4. Циклы")

L_04_for = Lesson(
    id="04-for-loop",
    section_id=S4[0],
    section_title=S4[1],
    title="for: пройти по элементам",
    body_md="""\
**Цикл `for`** — берёт по одному элементу из коллекции и выполняет код. По-русски:
«для каждого X в Y делай Z».

```python
fruits = ["яблоко", "груша", "банан"]
for fruit in fruits:
    print("Купил", fruit)
```

`fruit` — это **переменная цикла**. Имя любое — Python будет брать в неё каждый
элемент.

### range — последовательность чисел

`range(n)` даёт числа от `0` до `n-1`:

```python
for i in range(5):
    print(i)  # 0, 1, 2, 3, 4
```

`range(start, stop)`:

```python
for i in range(2, 7):
    print(i)  # 2, 3, 4, 5, 6
```

`range(start, stop, step)`:

```python
for i in range(0, 10, 2):
    print(i)  # 0, 2, 4, 6, 8
```

### Зачем нужен i

Если нужно одновременно знать **номер** элемента и сам элемент — `enumerate`:

```python
fruits = ["яблоко", "груша"]
for i, name in enumerate(fruits, start=1):
    print(i, name)  # 1 яблоко / 2 груша
```

### break и continue

- `break` — выйти из цикла досрочно.
- `continue` — пропустить остаток текущей итерации и перейти к следующей.

```python
for n in range(10):
    if n == 5:
        break        # дойдёт до 5 и остановится
    if n % 2 == 0:
        continue     # чётные пропустит
    print(n)
```
""",
    try_it_code='''for i in range(1, 11):
    print(i, end=" ")
print()  # перевод строки
total = 0
for n in [1, 2, 3, 4, 5]:
    total += n
print("Сумма:", total)
''',
    task_ids=(),
    xp=6,
)

L_04_while = Lesson(
    id="04-while",
    section_id=S4[0],
    section_title=S4[1],
    title="while: пока условие истинно",
    body_md="""\
**Цикл `while`** — выполняй блок, **пока условие истинно**.

```python
n = 5
while n > 0:
    print(n)
    n = n - 1
print("Поехали!")
```

### Чем `while` отличается от `for`

- `for` — когда заранее знаешь, **по чему** идти (по списку, по `range`).
- `while` — когда заранее не знаешь, **сколько раз** надо.

Например, угадайка: «спрашивай, пока не угадает» — это `while`.

### Берегись бесконечных циклов

```python
n = 5
while n > 0:
    print(n)
# забыли уменьшить n — это бесконечный цикл!
```

В нашем тренажёре сработает таймаут (5 сек) и он остановится. На реальной машине —
программа повиснет.

### Совет

В большинстве случаев `for` понятнее, чем `while`. Используй `while`, когда условие
выхода **зависит от пользователя или от внешних данных**.
""",
    try_it_code='''import random
secret = random.randint(1, 5)
guess = 0
attempts = 0
while guess != secret:
    attempts += 1
    guess = random.randint(1, 5)
print("Угадали число", secret, "за", attempts, "попыток")
''',
    task_ids=("t-guess-number",),
    xp=6,
)

L_04_nested = Lesson(
    id="04-nested-loops",
    section_id=S4[0],
    section_title=S4[1],
    title="Вложенные циклы",
    body_md="""\
В цикл можно положить ещё один цикл. Получим **вложение**.

```python
for i in range(1, 4):
    for j in range(1, 4):
        print(f"{i}*{j}={i*j}", end="\\t")
    print()  # перенос строки в конце ряда
```

Получится таблица умножения 3 на 3.

### Когда нужны

- Перебор пар «всё со всем» (карты в колоде, точки на сетке).
- Обработка вложенных списков (список списков).

### Сложность

Вложенный цикл «работает» **столько раз, сколько произведение длин**. Два цикла по
1000 элементов = миллион итераций. Это уже заметно — будь аккуратен.

### Как НЕ запутаться

- Имена переменных циклов разные (`i`, `j`, `row`, `col`).
- Внутренний `break` ломает только внутренний цикл, не оба.
""",
    try_it_code='''for row in range(1, 6):
    for col in range(1, 6):
        print(row * col, end="\\t")
    print()
''',
    task_ids=("t-multi-table",),
    xp=5,
)


# ----- Раздел 5: Функции -----

S5 = ("05-funcs", "5. Функции")

L_05_def = Lesson(
    id="05-def-return",
    section_id=S5[0],
    section_title=S5[1],
    title="Функции: def, return, print vs return",
    body_md="""\
Функция — кусок кода с именем. Ты её **определяешь** один раз и **вызываешь** сколько
угодно.

```python
def greet(name):
    print(f"Привет, {name}!")

greet("Кирилл")
greet("Аня")
```

- `def` — ключевое слово, начинаем определение функции.
- `greet` — **имя** функции.
- В скобках — **параметры** (входы).
- Тело — со сдвигом (4 пробела).

### return vs print

Это разные вещи!

- `print(x)` **показывает** `x` пользователю на экране. Возвращает `None`.
- `return x` **возвращает** значение туда, откуда вызвали. Снаружи можно его поймать
  в переменную.

```python
def double(n):
    return n * 2

result = double(5)   # вернётся 10
print(result)        # выведет 10
```

Если перепутать:

```python
def double_bad(n):
    print(n * 2)   # просто печатает на экран
    # return нет! значит, вернётся None

x = double_bad(5)  # на экран попадёт 10
print(x)           # выведет None
```

**Запомни**: пользователю — `print`. Дальше по коду — `return`.

### Параметры по умолчанию

```python
def greet(name, greeting="Привет"):
    print(f"{greeting}, {name}!")

greet("Кирилл")              # Привет, Кирилл!
greet("Кирилл", "Здарова")   # Здарова, Кирилл!
```
""",
    try_it_code='''def double(n):
    return n * 2

print(double(5))
print(double(7))

def greet(name, greeting="Привет"):
    print(f"{greeting}, {name}!")

greet("Кирилл")
greet("Аня", "Здарова")
''',
    task_ids=("t-area",),
    xp=7,
)

L_05_args = Lesson(
    id="05-args-kwargs",
    section_id=S5[0],
    section_title=S5[1],
    title="*args и **kwargs",
    body_md="""\
Бывает, что хочется передать **сколько угодно** аргументов.

### `*args` — много позиционных

```python
def my_sum(*nums):
    total = 0
    for n in nums:
        total += n
    return total

print(my_sum(1, 2, 3))           # 6
print(my_sum(10, 20, 30, 40))    # 100
```

`*nums` означает «сложи все позиционные аргументы в кортеж под именем `nums`». Имя
любое, но по соглашению пишут `args`.

### `**kwargs` — много именованных

```python
def show_user(**fields):
    for key, value in fields.items():
        print(f"{key}: {value}")

show_user(name="Кирилл", age=19)
# name: Кирилл
# age: 19
```

`**fields` означает «собери все именованные аргументы в словарь». Имя любое,
по соглашению — `kwargs`.

### Обратное распаковывание

`*` и `**` работают и в обратную сторону — **распаковывают**:

```python
nums = [1, 2, 3]
print(my_sum(*nums))   # как my_sum(1, 2, 3)

opts = {"name": "Аня", "age": 20}
show_user(**opts)      # как show_user(name="Аня", age=20)
```

### Зачем тебе это

Когда дойдём до Telegram-бота — увидишь, что библиотеки часто принимают `**kwargs`,
чтобы не ломаться от новых параметров. Знать имена `args/kwargs` — must.
""",
    try_it_code='''def my_sum(*nums):
    return sum(nums)

print(my_sum(1, 2, 3))
print(my_sum(10, 20, 30, 40))

def show_user(**fields):
    for k, v in fields.items():
        print(f"{k}: {v}")

show_user(name="Кирилл", age=19)
''',
    task_ids=(),
    xp=6,
)

L_05_lambda = Lesson(
    id="05-lambda",
    section_id=S5[0],
    section_title=S5[1],
    title="lambda: короткие функции на лету",
    body_md="""\
`lambda` — короткая запись функции, у которой **одно выражение**.

```python
square = lambda x: x * x
print(square(5))   # 25
```

Это **то же самое**, что:

```python
def square(x):
    return x * x
```

### Где встретишь

Когда нужно передать **функцию как аргумент** — `lambda` сокращает запись:

```python
nums = [3, 1, 4, 1, 5, 9, 2]
sorted_nums = sorted(nums, key=lambda x: -x)  # сортируем по убыванию
print(sorted_nums)
```

Но если функция длинная или используется в нескольких местах — пиши обычный `def`.
`lambda` — для одноразовых.

### Совет

`lambda` пугает только из-за слова. Не бойся: «лямбда — это короткая безымянная
функция в одну строку». Всё.
""",
    try_it_code='''cube = lambda x: x ** 3
print(cube(2))
print(cube(3))

users = [{"name": "Аня", "age": 20}, {"name": "Боб", "age": 18}]
users.sort(key=lambda u: u["age"])
print(users)
''',
    task_ids=(),
    xp=4,
)


# ----- Раздел 6: Файлы и модули -----

S6 = ("06-files", "6. Файлы и модули")

L_06_modules = Lesson(
    id="06-modules",
    section_id=S6[0],
    section_title=S6[1],
    title="Модули: import",
    body_md="""\
**Модуль** — отдельный файл с кодом. Чтобы воспользоваться чужим (или своим в другом
файле) — пишут `import`.

```python
import math

print(math.pi)            # 3.14159...
print(math.sqrt(16))      # 4.0
```

`math` — модуль из стандартной библиотеки Python. **Стандартная библиотека** — то,
что идёт «в комплекте» с Python: `math`, `random`, `json`, `datetime`, `os`...

### Разные формы import

```python
import math                  # math.sqrt
from math import sqrt        # просто sqrt
from math import sqrt as sq  # sq
import math as m              # m.sqrt
```

Любой вариант — рабочий. Выбирают по вкусу и читаемости.

### Свой модуль

Если у тебя в той же папке файл `helpers.py` с функцией `greet`, импортируешь так:

```python
from helpers import greet
greet("Кирилл")
```

Это и есть «разбить код по файлам».

### Полезные модули из стандартной библиотеки

| Модуль | Что внутри |
|---|---|
| `math` | синусы, корни, число π |
| `random` | случайные числа, перемешивание |
| `datetime` | даты и время |
| `json` | сохранять/читать JSON |
| `os`, `pathlib` | файлы и папки |
""",
    try_it_code='''import random

names = ["Аня", "Боб", "Веня"]
print("Случайный:", random.choice(names))

import math
print("Пи:", math.pi)
print("Корень из 81:", math.sqrt(81))
''',
    task_ids=(),
    xp=5,
)

L_06_files = Lesson(
    id="06-files-json",
    section_id=S6[0],
    section_title=S6[1],
    title="Файлы и JSON",
    body_md="""\
Чтобы записать что-то на диск или прочесть — используют `open` с конструкцией `with`.

### Запись

```python
with open("notes.txt", "w", encoding="utf-8") as f:
    f.write("Привет, файл!\\n")
```

`"w"` означает «write» (запись, перезаписать). После выхода из `with` файл
автоматически закроется. **Всегда** используй `with` — иначе можно забыть `close`.

### Чтение

```python
with open("notes.txt", "r", encoding="utf-8") as f:
    text = f.read()
print(text)
```

Хочешь по строкам?

```python
with open("notes.txt", "r", encoding="utf-8") as f:
    for line in f:
        print(line.rstrip())
```

### JSON

JSON — стандартный формат данных. Похож на Python-словарь и список. Любой бот,
любой API — это JSON.

```python
import json

user = {"name": "Кирилл", "age": 19, "skills": ["python"]}
text = json.dumps(user, ensure_ascii=False, indent=2)
print(text)

restored = json.loads(text)
print(restored["name"])
```

`json.dumps` — словарь → строка. `json.loads` — строка → словарь.

`json.dump` / `json.load` — то же, но сразу с файлом.

```python
with open("user.json", "w", encoding="utf-8") as f:
    json.dump(user, f, ensure_ascii=False, indent=2)

with open("user.json", "r", encoding="utf-8") as f:
    data = json.load(f)
```
""",
    try_it_code='''import json

user = {"name": "Кирилл", "age": 19}
text = json.dumps(user, ensure_ascii=False, indent=2)
print(text)

back = json.loads(text)
print("Имя:", back["name"])
''',
    task_ids=(),
    xp=6,
)


# ----- Раздел 7: pip / venv / API -----

S7 = ("07-pip-api", "7. pip, виртуальное окружение, API")

L_07_pip = Lesson(
    id="07-pip-venv",
    section_id=S7[0],
    section_title=S7[1],
    title="pip и виртуальное окружение",
    body_md="""\
Кроме стандартной библиотеки, в Python есть **тысячи внешних** — в репозитории
[PyPI](https://pypi.org). Их ставят командой `pip`.

```text
pip install requests
```

После этого в коде:

```python
import requests
r = requests.get("https://example.com")
print(r.status_code)
```

### Виртуальное окружение (venv)

Если ты ставишь библиотеки прямо в систему — со временем будет «суп» из несовместимых
версий. Поэтому делают **виртуальное окружение** — папку с локальными библиотеками
для конкретного проекта.

В терминале (Windows):

```text
python -m venv .venv
.venv\\Scripts\\activate
pip install requests
```

(на Linux/macOS вместо `.venv\\Scripts\\activate` — `source .venv/bin/activate`)

Активировано — все `pip install` пойдут только в этот проект. Удалил папку — нет
больше зависимостей.

### requirements.txt

Чтобы коллега (или ты сам через год) мог поставить **тот же** набор библиотек —
сохраняют список:

```text
pip freeze > requirements.txt
```

И ставят:

```text
pip install -r requirements.txt
```

Внутри файла — построчно `имя==версия`:

```text
requests==2.31.0
aiogram==3.4.1
```

### Зачем тебе это

В разделе про Telegram-бота поставим `aiogram`. Без `pip` его взять негде.

> ⚠ В нашем тренажёре нельзя устанавливать пакеты напрямую — это бы нарушило
> песочницу. Поэтому код с внешними библиотеками здесь **не запустится**, но
> понимать команды надо: ты будешь применять их у себя в терминале/IDE.
""",
    try_it_code='print("pip и venv можно потренировать только в реальном терминале — но эти команды теперь тебе знакомы.")\n',
    task_ids=(),
    xp=5,
)

L_07_api = Lesson(
    id="07-http-api",
    section_id=S7[0],
    section_title=S7[1],
    title="HTTP-запросы и API",
    body_md="""\
**API** (Application Programming Interface) — способ одной программы попросить что-то
у другой. Большинство современных API работают через HTTP — те же URL, что и у
сайтов, только в ответ приходит обычно **JSON**, а не HTML.

### Простой пример с requests

```python
import requests

r = requests.get("https://api.thecatapi.com/v1/images/search")
data = r.json()
print(data[0]["url"])
```

Что произошло:

1. `requests.get(url)` — отправили GET-запрос.
2. `r.json()` — Python сам распарсил JSON-ответ в список словарей.
3. Достали `[0]["url"]` — ссылку на картинку котика.

### GET, POST и параметры

- `GET` — «дай мне данные».
- `POST` — «прими от меня данные».

Параметры:

```python
requests.get("https://api.example.com/search", params={"q": "python"})
# превратится в https://api.example.com/search?q=python
```

POST с JSON:

```python
requests.post("https://api.example.com/users",
              json={"name": "Кирилл"})
```

### Зачем тебе это

Telegram-бот — это, по сути, программа, которая ходит в Telegram API за новыми
сообщениями и шлёт ответы туда же. Только для удобства — есть готовая библиотека
`aiogram`, которая прячет HTTP-детали.

> ⚠ В нашем тренажёре нет интернета внутри песочницы, но в реальной IDE этот код
> заработает с пакетом `requests`.
""",
    try_it_code='print("Здесь обычно требуется requests — у нас в песочнице его нет, но команда тебе уже знакома.")\n',
    task_ids=(),
    xp=5,
)


# ----- Раздел 8: Telegram-бот -----

S8 = ("08-bot", "8. Первый Telegram-бот")

L_08_create = Lesson(
    id="08-bot-create",
    section_id=S8[0],
    section_title=S8[1],
    title="Регистрируем бота у BotFather",
    body_md="""\
Бот в Telegram — это аккаунт, которым управляет программа, а не человек. Чтобы его
создать, не надо ничего изобретать — есть **специальный бот** `@BotFather`.

### Шаги

1. Откройте Telegram, найдите [@BotFather](https://t.me/BotFather), нажмите **Start**.
2. Отправьте команду `/newbot`.
3. Введите **имя** бота (любое, можно по-русски).
4. Введите **username** бота — должно заканчиваться на `bot`, например `my_first_pytrainer_bot`.
5. BotFather пришлёт **токен** — длинная строка вида `123456:ABC-DEF...`. Это **пароль**
   твоего бота.

### Береги токен

- Никогда не показывай его в репозитории, в скриншотах, в чате.
- Если случайно показал — `/revoke` у BotFather и сделай новый.

### Куда положить токен в коде

В рабочем проекте — в **переменную окружения** или в `.env`-файл, не зашивай прямо
в код:

```python
import os
TOKEN = os.environ["BOT_TOKEN"]
```

Или, если только для пробы:

```python
TOKEN = "СЮДА_ТОКЕН"
```

(потом не забыть сменить).

### Что мы сделаем дальше

Поставим библиотеку `aiogram`, напишем бота, который отвечает на любое сообщение тем
же текстом — это «эхо-бот». Минимум кода — максимум радости.
""",
    try_it_code='print("На этом уроке кода нет — нужно зайти в Telegram и поговорить с @BotFather.")\n',
    task_ids=(),
    xp=4,
)

L_08_echo = Lesson(
    id="08-bot-echo",
    section_id=S8[0],
    section_title=S8[1],
    title="Эхо-бот на aiogram",
    body_md="""\
**aiogram** — самая популярная Python-библиотека для Telegram-ботов. Под капотом —
`asyncio` (асинхронный код). Не пугайся слов: пока что просто увидим **работающий**
шаблон.

### Установка

```text
python -m venv .venv
.venv\\Scripts\\activate         # на Linux/Mac: source .venv/bin/activate
pip install aiogram
```

### Минимум кода

```python
import asyncio
from aiogram import Bot, Dispatcher
from aiogram.filters import CommandStart
from aiogram.types import Message

TOKEN = "СЮДА_ТОКЕН_ОТ_BOTFATHER"

bot = Bot(TOKEN)
dp = Dispatcher()


@dp.message(CommandStart())
async def on_start(message: Message) -> None:
    await message.answer("Привет! Я твой первый бот. Напиши что-нибудь — я повторю.")


@dp.message()
async def echo(message: Message) -> None:
    if message.text:
        await message.answer(message.text)


async def main() -> None:
    await dp.start_polling(bot)


if __name__ == "__main__":
    asyncio.run(main())
```

### Что происходит

- `Bot(TOKEN)` — клиент, который умеет говорить с Telegram.
- `Dispatcher` — «маршрутизатор» сообщений.
- Декоратор `@dp.message(...)` — «эту функцию вызвать, когда придёт нужное сообщение».
- `CommandStart()` — фильтр на команду `/start`.
- `await message.answer(...)` — отвечаем тем же чатом.
- `dp.start_polling(bot)` — запускает «опрос» Telegram (каждые несколько секунд
  спрашиваем «есть новые сообщения?»).

### `async`/`await` — зачем

Бот может одновременно ждать ответы от Telegram и отвечать другим пользователям.
`async`/`await` — способ сказать «эта функция может ждать». Подробно — в продвинутых
уроках; пока что **достаточно скопировать** этот шаблон.

### Запуск

```text
python bot.py
```

Открой бота в Telegram, нажми Start — увидишь свою же программу в действии.
""",
    try_it_code='print("Этот код запускают в реальной системе — у нас в песочнице нет интернета и aiogram.")\n',
    task_ids=("t-bot-echo",),
    xp=8,
)

L_08_commands = Lesson(
    id="08-bot-commands",
    section_id=S8[0],
    section_title=S8[1],
    title="Команды и куда расти дальше",
    body_md="""\
К твоему боту легко добавлять команды. Каждая команда — отдельная функция:

```python
from aiogram.filters import Command


@dp.message(Command("help"))
async def on_help(message: Message) -> None:
    await message.answer(
        "Команды:\\n"
        "/start — приветствие\\n"
        "/help — этот список\\n"
        "/cat — случайный котик"
    )


@dp.message(Command("cat"))
async def on_cat(message: Message) -> None:
    import requests
    url = requests.get(
        "https://api.thecatapi.com/v1/images/search"
    ).json()[0]["url"]
    await message.answer_photo(url)
```

### Куда расти

- **Кнопки** (`InlineKeyboardMarkup`) — клавиатуры, которые появляются под сообщением.
- **Состояния FSM** — запоминать, на каком шаге диалога пользователь.
- **База данных** (`sqlite`) — чтобы пользователи и их данные «жили» между запусками.
- **Размещение бота на сервере** — чтобы он работал, когда твой ноут выключен.

### Где смотреть

- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

### Что мы прошли

Поздравляю — ты прошёл от `print("Hello")` до запуска первого Telegram-бота.
Дальше — практика и собственные идеи. Возвращайся в «Песочницу», эксперементируй,
читай документацию. **Всё**, что мы видели в курсе, — реальные кирпичи реальных
проектов.
""",
    try_it_code=None,
    task_ids=(),
    xp=4,
)


LESSONS: tuple[Lesson, ...] = (
    L_00_how_to_learn,
    L_00_what,
    L_00_print,
    L_00_errors,
    L_01_vars,
    L_01_types,
    L_01_input,
    L_02_if,
    L_02_elif,
    L_02_bool,
    L_03_lists,
    L_03_dicts,
    L_04_for,
    L_04_while,
    L_04_nested,
    L_05_def,
    L_05_args,
    L_05_lambda,
    L_06_modules,
    L_06_files,
    L_07_pip,
    L_07_api,
    L_08_create,
    L_08_echo,
    L_08_commands,
)
